# ═══════════════════════════════════════════════════════════════
#                    TrendRadar 配置文件
#                      Version: 2.0.0
# ═══════════════════════════════════════════════════════════════

# 可视化配置编辑器地址: https://sansan0.github.io/TrendRadar/

# ===============================================================
# 1. 基础设置
# ===============================================================
app:
  timezone: "Asia/Shanghai"
  show_version_update: true

# ===============================================================
# 1.5 调度系统
# ===============================================================
schedule:
  enabled: true
  preset: "morning_evening"

# ===============================================================
# 2. 数据源 - 热榜平台
# ===============================================================
platforms:
  enabled: true
  sources:
    - id: "toutiao"
      name: "今日头条"
    - id: "baidu"
      name: "百度热搜"
    - id: "wallstreetcn-hot"
      name: "华尔街见闻"
    - id: "thepaper"
      name: "澎湃新闻"
    - id: "cls-hot"
      name: "财联社热门"
    - id: "weibo"
      name: "微博"
    - id: "zhihu"
      name: "知乎"

# ===============================================================
# 3. 数据源 - RSS 订阅（精简版：仅保留可用/核心源）
# ===============================================================
rss:
  enabled: true
  freshness_filter:
    enabled: true
    max_age_days: 1  # 全局改为1天，聚焦最新资讯

  feeds:
    # 🇺🇸 美国股票财经（仅保留实测可用的核心源）
    - id: "cnbc-markets"
      name: "CNBC 美股市场"
      url: "https://www.cnbc.com/id/100003114/device/rss/rss.html"
      max_age_days: 1  # 美股新闻时效性极强

    # 🌍 世界 IT 科技（仅保留实测可用的核心源）
    - id: "the-verge"
      name: "The Verge 全球科技"
      url: "https://www.theverge.com/rss/index.xml"
      max_age_days: 1  # 科技新闻只看最新

    # 🇭🇰 香港本地新闻（保留1个最稳定的源）
    - id: "rthk-finance"
      name: "RTHK 香港财经新闻"
      url: "https://rthk.hk/rthk/news/rss/c_expressnews_cfinance.xml"
      max_age_days: 1

# ===============================================================
# 4. 报告模式
# ===============================================================
report:
  mode: "current"
  display_mode: "keyword"
  sort_by_position_first: false
  rank_threshold: 5
  max_news_per_keyword: 0

# ===============================================================
# 5. 推送内容控制
# ===============================================================
display:
  region_order:
    - hotlist
    - rss
    - ai_analysis

  regions:
    hotlist: true
    new_items: false
    rss: true
    standalone: false
    ai_analysis: true

  standalone:
    platforms: ["zhihu", "wallstreetcn-hot"]
    rss_feeds: ["the-verge","rthk-finance","cnbc-markets"]
    max_items: 10  # 精简独立展示条数

# ===============================================================
# 6. 推送通知
# ===============================================================
notification:
  enabled: true
  channels:
    feishu:
      webhook_url: ""
    dingtalk:
      webhook_url: ""
    wework:
      webhook_url: ""
      msg_type: "markdown"
    telegram:
      bot_token: ""
      chat_id: ""
    email:
      from: ""
      password: ""
      to: ""
      smtp_server: ""
      smtp_port: ""

# ===============================================================
# 7. 存储配置
# ===============================================================
storage:
  backend: "auto"
  formats:
    sqlite: true
    txt: false
    html: true
  local:
    data_dir: "output"
    retention_days: 7  # 本地保留7天数据即可

# ===============================================================
# 8. AI 模型配置
# ===============================================================
ai:
  model: "deepseek/deepseek-chat"
  api_key: ""
  api_base: ""
  timeout: 60  # 缩短超时，提升响应速度
  temperature: 0.7  # 降低随机性，分析更精准
  max_tokens: 2000  # 精简AI输出，聚焦核心

# ===============================================================
# 9. AI 分析功能
# ===============================================================
ai_analysis:
  enabled: true
  language: "Chinese"
  prompt_file: "ai_analysis_prompt.txt"
  mode: "current"
  max_news_for_analysis: 50  # 大幅减少分析条数，降低成本
  include_rss: true
  include_standalone: false
  include_rank_timeline: false  # 关闭冗余时间线，减少token消耗

# ===============================================================
# 10. AI 翻译功能
# ===============================================================
ai_translation:
  enabled: true

# ===============================================================
# 11. 高级设置
# ===============================================================
advanced:
  debug: false
  crawler:
    request_interval: 3000
    use_proxy: false
    default_proxy: "http://127.0.0.1:10801"
  rss:
    request_interval: 2000
    timeout: 10
    use_proxy: false
  weight:
    rank: 0.7
    frequency: 0.2
    hotness: 0.1
  max_accounts_per_channel: 2
  batch_size:
    default: 3000
    dingtalk: 15000
    feishu: 20000
  batch_send_interval: 2
